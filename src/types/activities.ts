export interface ActivityPlate {
  name: string,
  description: string,
  price: number,
  currency: string,
  src: string,
  alt: string,
}

export interface BasicPrice {
  days: number;
  price: number;
}
